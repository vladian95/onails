// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // Включаем поддержку новой структуры приложения Next.js
  },
};

module.exports = nextConfig;
